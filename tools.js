'use strict';

module.exports.arrayToReplies = function ( arr) {
  //return arr.map( function (el) { return {type: 'text', content:el} } );
  return arr.map( el =>   {}  ); //{type: 'text', content:'el'}
  
}